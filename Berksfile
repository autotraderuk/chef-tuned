source 'https://supermarket.chef.io'

# This is a reference to the metadata.rb file contained within this repository
metadata

group :integration do
  cookbook 'test_tuned', :path => './test/cookbooks/test_tuned'
end
